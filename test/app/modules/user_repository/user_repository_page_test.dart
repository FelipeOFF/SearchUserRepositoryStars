import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:github_repository_stars/app/modules/user_repository/user_repository_page.dart';

void main() {
  testWidgets('UserRepositoryPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(UserRepositoryPage(title: 'UserRepository')));
    //  final titleFinder = find.text('UserRepository');
    //  expect(titleFinder, findsOneWidget);
  });
}
